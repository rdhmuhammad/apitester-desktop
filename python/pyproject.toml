[project]
name = "apitester-desktop"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "ridhomuhammadasli", email = "5mblackburn7@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "app>=0.0.1",
    "dotenv>=0.9.9",
    "flask>=3.1.3",
]

[project.scripts]
apitester-desktop = "apitester_desktop:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
